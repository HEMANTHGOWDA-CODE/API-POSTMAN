buf = new Buffer(10);
a = buf.write('Welcome');
console.log(a);

buf = new Buffer.from("Welconme");
json = buf.toJSON(buf);
console.log(json);

buf1 = new Buffer.from('company : ');
buf2 = new Buffer.from('Accenture');
join = Buffer.concat([buf1,buf2]);
console.log(join.toString());